# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

Static personal portfolio website (Piotr Jeleniewicz / "Xcape"), built by customizing the "Isti" ThemeForest HTML template, extended with custom electronics-themed canvas parallax backgrounds. Plain HTML/CSS/JS - no build system, package manager, linter, or test suite.

**Backups (never delete, never edit):**
- `C:\Users\user1\Documents\GitHub\stronus_backup_2026-07-03\` - the design before effects were added (template look, no parallax).
- `C:\Users\user1\Documents\GitHub\stronus_backup_2026-07-03_v2-efekty\` - the first effects version (spectrum under content, telescope dish + signal rings still present).
- `C:\Users\user1\Documents\GitHub\stronus_backup_2026-07-03_v3\` - version with the wave behind About and the spectrum band at the bottom of Services (both removed afterwards).
- `C:\Users\user1\Documents\GitHub\stronus_backup_2026-07-03_v4-led\` - version with the tuned LED band, before the contact-section redesign (old "LET'S GET IN TOUCH!" h4s, template absolute-positioned EMAIL ME).
- `C:\Users\user1\Documents\GitHub\stronus_backup_brutal_2026-07-03\` - a rejected full "brutal" redesign (index.html + site.css + site.js), kept for reference.
- Rule from the user: before any major design edit, make a fresh backup copy first - a pristine backup must always exist.

## Development

- No build step. Serve the repo root with any static server (e.g. `python -m http.server`).
- Deployment is automatic: `.github/workflows/static.yml` publishes the entire repository root to GitHub Pages on every push to `main`. Everything committed goes live.
- To verify visually, Playwright is available in `C:\Users\user1\Documents\ROBOTA\node_modules` (`@playwright/test`); run screenshot scripts with module resolution inside that folder (copy the script there first).

## Structure

- `index.html` - single page. Sections: banner (D3 force graph + intro, fixed gradient `banner_bg.jpg`), `#about` (bio + image gallery, no effects), oscilloscope separator, `#service` (skills cards; heading kicker "Core Strengths" / title "From Circuits to Code" - replaced the stock template's "Pushing Boundaries: My Portfolio of Limitless Ideas" corporate-agency fluff; card photos `images/service_2/3.jpg` are real, freely-licensed Unsplash photos matching each card's theme - a code editor for Programming, a server rack for IT Tools; the "Electronics and Simulations" card instead got the `.tf__ph_gallery` treatment reused from the project galleries - `#gal-electronics` cycles through 5 of the user's own real photos (`images/projects/plain_axi4_lite.png` Vivado AXI4-Lite waveform sim, `anddd.png`/`image.png` standard-cell IC layout views, `FLKC9BXIWMQUJ37.png` a Basys 3 FPGA board on a breadboard, `Screenshot_13.png` a CNC engraving machine milling the PG ETI logo - all just "photos for Circuits to Code", not treated as their own project entry per the user's explicit instruction). It needed a third gallery mode, `.tf__ph_gallery--fixed`: `.tf__single_service_img` is already a fixed 310px `position:relative` box (template CSS), so this mode skips `sizeGalleryTo` entirely in effects.js (`g.classList.contains("tf__ph_gallery--fixed")` early-return) and lets CSS just fill it at 100%/100% with `object-fit:cover`, since the 5 source images have wildly different aspect ratios and dynamic width/height resizing (the jobagg/passmgr approach) would make the card visibly jump size on every cycle - this mode intentionally allows cropping, unlike the project galleries). The LED-matrix scroll band that used to sit between `#service` and `#edu` (`#fx-led-band`/`#fx-led` canvas, P4 effect) was removed at the user's request - too many effects stacked on the page, it scrolled too fast to read, and it only ever showed a loose list of buzzwords with no real content behind it. `#edu` (education timeline, **P6 starfield only**), `#projects` (3 main projects as full-width alternating hero rows `.tf__project_hero`, ids `#p-labinc`/`#p-jobagg`/`#p-passmgr`: text column + media column; all three use real screenshots, same single bounding box as the original LabInc treatment: LabInc is a plain `<img>` (`images/projects/labinc.png`, from the LabInc repo's `docs/`); Job Aggregator (`#gal-jobagg`) and Password Manager (`#gal-passmgr`) use `.tf__ph_gallery` - multiple `<img>`s stacked absolutely in one box (`aspect-ratio` locked to the first image's ratio, others `object-fit:cover`), only `.is-active` visible, cross-fading; scrolling the mouse wheel while over the box cycles through them with fade in/out (`wheel` handler in effects.js, `preventDefault`+cooldown while there's a next/prev image to switch to; at the first/last image the event is left alone so the page scrolls normally instead of getting stuck - needs `data-lenis-prevent` on the gallery div, otherwise Lenis's own wheel handling fights the image-switch logic and the page scrolls instead of the image changing; skipped on reduced-motion) with small `.tf__ph_gallery_dots` showing position - jobagg: main table / Gemini deep-research panel / tech-stack dictionary; passmgr: vault list+entry detail / security dashboard - user-supplied, self-hosted; images use `object-fit: contain !important` (the global base-CSS `img{object-fit:cover!important}` must be beaten with `!important` too, plain specificity isn't enough) so nothing is cropped; instead of letterbox bars, `.tf__ph_gallery` normalizes one axis and lets effects.js dynamically resize the other per active image (`naturalWidth`/`naturalHeight`, with a CSS transition) on load and on every gallery `show()` - default mode (passmgr, text on its LEFT) fixes height and grows width away from the text (block-left-aligned, so the edge near the text never moves) - passmgr additionally carries `.tf__ph_gallery--wide` (a 1.16x width multiplier in `sizeGalleryTo` plus `object-fit:cover; object-position:top` instead of `contain`) so it's a bit wider than its natural ratio and crops slightly off the bottom rather than staying letterboxed; `#gal-jobagg` uses the `.tf__ph_gallery--fit-h` modifier instead (text is on ITS right, `flex-lg-row-reverse`) which fixes width to 100% of the column (same gap to text as LabInc's plain image) and grows HEIGHT instead, so neither the left nor the right edge ever moves as the gallery cycles - only the box's height breathes; clicking any project screenshot (gallery or the plain LabInc image) opens it full-size in a `#fx-lightbox` overlay, closed by clicking it again or Escape), `#online` (side projects: Yaper, Radio Telescope Control Panel, Florist Website, BlackBox, Stock Tool, Inventory Generator - the earlier "This Portfolio" self-referential entry was dropped since the site itself already demonstrates it; the speech-to-text app is spelled "Yaper" with a Y, not "Japper"), `#about-me` (bio + tool chips; `bio.intro`'s headline trait is structural thinking - breaking problems down like a tree of category/subcategory/connections - with automation and tool-chaining framed explicitly as a consequence of that mindset, not a separate skill, per the user's request; also carries `bg-window` per an explicit later request even though it wasn't originally designed to - the bio text and chip cloud sit in their own `--colorDarkAccent`-backed boxes so the fixed banner gradient only shows in the gaps around them, same visual pattern as `#projects`/`#online`), `#activity` ("My Activity": left - "Coding Activity" card: a real GitHub contribution graph (`images/gh-contributions.svg`, a static SVG fetched once from `ghchart.rshah.org/2c7ffe/Xcape53` with the zero-activity `fill:#EEEEEE` swapped to `#1d2024` to read correctly on the card's dark background - refresh command is in the HTML comment above the `<img>`) replacing the earlier `.cc-heatmap`/`.cc-day` block grid of Claude Code daily token volume, which the user felt looked sparse/empty; below it a one-line `.cc-summary` ("Shipping something most days..."), then a `.cc-mytools` block labelled "My Tools" with a two-column `.cc-mytools_row` of linked `.cc-tool-stat` boxes (Claude Code -> `claude.com/claude-code`, token count; Codex -> `chatgpt.com/codex`, a non-numeric "Everyday coding partner" line since Codex - now folded into the ChatGPT app - has no usage-export tool the way `ccusage` covers Claude Code) - this replaced the old plain-text token detail plus the separate "Built with Claude Code" link/button, which the user felt didn't read well; a `.cc-tools` chip row below still has "Also using: VS Code" (Codex chip dropped from here since it's now covered by its own `.cc-tool-stat`); right - "Right Now", a hand-written `.about-me-list` of what's currently keeping the user busy (thesis, SimLE, side projects, tutoring - mirrors the "What keeps me busy right now" list in `#about-me`) plus a "Also on GitHub" profile link. This replaced an earlier live-fetched "Latest on GitHub" (5 most-recent repos via the GitHub API, `#gh-repos` in effects.js) and, before that, a contribution calendar - both were dropped because the user's git activity is sparse and made the section look empty/bad; a hand-curated "what I'm doing" list can't ever look sparse since it doesn't depend on push frequency) - `#projects`+`#online`+`#about-me` carry class `bg-window` (the fixed banner gradient shows through, a full-width "hole" in the dark background; the +200px window padding sits on `#projects` top and, since `#activity` sits right after `#about-me` without `bg-window`, the closing padding-bottom hack sits on `#about-me`), oscilloscope separator, `#contact` (kicker + big "LET'S TALK" heading - the `.talk` span is a white `-webkit-text-stroke` outline that fills blue on hover; it MUST stay `font-family: Poppins` - Spline Sans glyphs have self-overlapping contours and text-stroke renders broken lines inside them (also override the global `span{font-size:16px}` rule) - plus magnetic EMAIL ME button; `#magzone` gets its capture area from padding + negative margin), footer. Nav: Home, Projects, CV (opens `cv/Piotr_Jeleniewicz_CV.pdf`), Contact.
- `js/effects.js` - custom, single rAF loop driving all added effects: LED matrix (`#fx-led`), starfield (`#fx-sky`), mouse-reactive oscilloscope separators (`.osc-sep` - a plain sine wave by default; near the cursor's x-position it morphs into a 4-term Fourier-series approximation of a square wave (odd harmonics 1,3,5,7, `fourierSquare()` in effects.js), blending linearly with the sine by distance from the cursor so only the area under/near the mouse squares off, Gibbs-phenomenon ripple and all), magnetic button (`#magzone`/`#mag`), project panel glow (a STATIC, local filled+blurred rounded-rect behind each whole `.tf__project_hero` panel - text AND screenshot, not just the image - colored per `--tint-rgb` CSS var copied once from the row's `.tf__project_hero[data-tint="r,g,b"]` attribute in effects.js: LabInc deep blue 0,122,204 / Job Aggregator teal 63,217,164 / Password Manager near-black 28,28,30; implemented as `::before { inset:0; border-radius:48px; background: rgba(var(--tint-rgb),.55); filter: blur(42px); }` - a solid color block blurred at the edges for a soft "scribble" halo, per the user's sketch, NOT a radial-gradient fading from center (tried and rejected - user wanted the center solid-colored like the rest of the box, not just the edges glowing); Job Aggregator overrides to a diagonal gradient (`#p-jobagg::before { background: linear-gradient(135deg, rgba(58,245,85,.55), rgba(41,237,129,.55)) }`), Password Manager overrides to an even darker/near-black fill and taller inset (`#p-passmgr::before { inset: 8% -4%; background: rgba(8,8,10,.75) }` vs the shared `20% -4%` / `rgba(var(--tint-rgb),.55)`); earlier a full-viewport `mix-blend-mode:screen` overlay (`#fx-tint`) recolored the whole page as you scrolled and was reverted for being too distracting site-wide), drifting clouds behind the LabInc hero (`#fx-clouds` canvas, first child of `#p-labinc.fx-host`, soft radial-gradient puffs some moving left some right at slow random speeds - matches the sky in the game's own screenshot; contained within the row's own bounding box like any other `.fx-bg` canvas - a full-viewport 100vw breakout was tried and reverted at the user's request; the canvas itself fades out at all four edges via a `mask-image` (two linear-gradients, one per axis, combined with `mask-composite: intersect` / `-webkit-mask-composite: source-in` for cross-browser support) so clouds crossing the boundary fade gradually instead of being hard-clipped by the canvas edge), `.tf__project_hero_text` is aligned toward whichever side the image sits on, per project (ID-scoped rules, not a shared class): `#p-labinc`/`#p-passmgr` (image on the right) get `text-align:right` + `justify-content:flex-end` on `.tf__project_card_head`/`.tf__project_tags` + `.about-me-list li { margin-left:auto; margin-right:0 }`; `#p-jobagg` (image on the left, `flex-lg-row-reverse`) gets the mirrored `text-align:left` / `flex-start` / `margin-right:auto`; `.about-me-list li` itself stays `text-align:left` and `width:fit-content` so each checkmark+line reads normally as one block - do NOT make the `li` itself `display:flex`, that broke inline content like `<sup>` by turning it into a separate flex item (tried and reverted); all three `.tf__project_hero` sections get `min-height: 68vh` so they occupy most of the viewport without excessive empty space above/below the content, reset to `auto` under 991px). The P3 wave and P2 spectrum were removed at the user's request ("za dużo" - too much at once); the telescope dish and signal rings likewise. Canvases draw only when near the viewport; `prefers-reduced-motion` renders one static frame. Every element lookup is null-guarded. Comments in Polish.
- `js/animacja-banner.js` - custom D3 force-graph hero animation (needs `js/d3.v7.min.js` and a `.tf__banner_img` element containing an `<svg>` with nonzero size). Comments/console messages in Polish - keep that convention here and in `effects.js`.
- `js/main.js` - template init script (jQuery IIFE, `isti` object) with local modifications (e.g. `updateGUTProgress`); edit carefully, don't replace with a stock copy. `js/animation.js` (Lenis), `js/plugin.js`, `js/d3.v7.min.js` are vendor - do not hand-edit.
- `css/style.css` - template styles plus custom blocks appended at the end (about-me chips, semester bar marks, `tf__project_card`, `tf__online_card`, and the FX block: `.fx-host`, `.fx-bg`, `.osc-sep`, `.fx-dish`). Other CSS files are vendor/utility.
- `cv/Piotr_Jeleniewicz_CV.pdf` - copy of an export from the CV project at `C:\Users\user1\Documents\ROBOTA` (source of truth, incl. structured profile data in `source/user_profile.json`). Regenerate there and re-copy to update.
- `inventoryGen/` - separate self-contained mini-app (Minecraft inventory generator, linked from the projects section). Leave it alone unless asked.
- `images/set/` - the user's own graphics (do not delete).

## Notes

- The `.barfiller .fill` progress bars in `#edu` (studies/internship "Completion" bars, vendor jQuery barfiller plugin) use `var(--colorSecondary)` (violet `#642efd`) instead of the template's original hardcoded green `#00a500` - changed at the user's request, two duplicate rule blocks in `css/style.css` (one per breakpoint) both needed updating.

## Conventions

- Content edits happen in `index.html`; anchors: `#top`, `#about`, `#service`, `#edu`, `#projects`, `#online`, `#about-me`, `#contact`.
- The site relies on template class prefixes `tf__*` and spacing utilities (`pt_100`, ...) from `css/spacing.css` - reuse them.
- FX pattern: a section gets class `fx-host`, a `<canvas class="fx-bg">` as first child, and a draw function registered in the `js/effects.js` rAF loop gated by `near(el)`. New effects must join that loop, not spawn their own.
- Do NOT use the template's `data-animation` GSAP reveal on elements in `#projects`/`#online` - their ScrollTriggers never fire there (cards stayed at opacity 0; root cause in main.js/Lenis not found, time-boxed). Use the custom `.fx-reveal` class instead: effects.js arms it (`html.fx-armed`) and reveals via IntersectionObserver; without JS or with reduced-motion the cards are simply visible.
- Projects in `#projects` mirror `C:\Users\user1\Documents\ROBOTA\source\user_profile.json` - keep in sync.
- Design history: the user rejected both glassmorphism "AI default" looks and a full brutal redesign; the agreed direction is the original template look + electronics-themed parallax (spectrum, LED matrix, radiotelescope, oscilloscope). Keep new elements in that language and subtle enough not to fight the template.
